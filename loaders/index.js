const userLoaders = require("./user");
const postLoaders = require("./post");
const commentLoader = require("./comment");

module.exports = {
  userLoaders,
  postLoaders,
  commentLoader
};
